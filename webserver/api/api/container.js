module.exports = function(req, res, next) {
    switch (req.method) {
        case "GET":
            switch (req.path.split("/")[1]) {
                
            }
            break
        case "POST":
            break
        case "DELETE":
            break
        case "PATCH":
            break
    }
}